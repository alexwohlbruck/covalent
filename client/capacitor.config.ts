import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wohlbruck.friendshiplamp',
  appName: 'friendship-lamp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
